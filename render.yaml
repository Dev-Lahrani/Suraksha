services:
  - type: web
    name: suraksha
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 8000
      # Optional — add these in the Render dashboard if you have them.
      # Without them the app still works (deterministic fallback templates).
      # - key: NUGEN_API_KEY
      #   sync: false
      # - key: OPENAI_API_KEY
      #   sync: false
      # - key: WHATSAPP_TOKEN
      #   sync: false
      # - key: WHATSAPP_PHONE_NUMBER_ID
      #   sync: false
